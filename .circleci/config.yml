version: 2
jobs:
  build:
    working_directory: ~/tmp
    docker:
      - image: circleci/node:4.8.4
#      - image: mongo:3.4.4
    steps:
      - checkout
      - run: curl https://install.meteor.com | sh
      - run: export LC_ALL=en_ZA.UTF-8
      - run: meteor npm install
      - run: meteor test --once --driver-package dispatch:mocha
